[workspace]
resolver = "3"
members = ["_docs", "app", "metw-signin", "service", "state", "tests"]

[workspace.dependencies]
metw-id = {
    git = "https://github.com/metwse/metw-backend-crates",
    package = "metw-id",
    tag = "metw-id-v0.1.1",
}
metw-server = {
    git = "https://github.com/metwse/metw-backend-crates",
    package = "metw-server",
    tag = "metw-server-v0.1.0",
}

# async runtime&utility
tokio = "1"
tower-http = "0.7"
async-trait = "0.1"
axum = "0.8"

utoipa = "5.5"

# repository
sqlx = "0.9"
chrono = "0.4"
redis = "1"

# observability
tracing = "0.1"
tracing-subscriber = "0.3"

# utility macros
pastey = "0.2"
thiserror = "2"

dotenvy = "0.15"
envy = "0.4"

# field verification
validator = "0.20"
regex = "1.12"

serde = "1"
serde_with = "3.2"
serde_json = "1"
serde_variant = "0.1.3"
base64 = "0.23"

# [dev-dependencies]
test-log = "0.2"
futures-util = "0.3"

[workspace.lints.rust]
unsafe_code = "forbid"
unused_must_use = "forbid"
missing_docs = "warn"

[workspace.lints.clippy]
all = "warn"
