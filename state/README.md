# metw-accounts-center state
Set `DATABASE_URL` and `REDIS_URL` environment variables to run production
database tests. Check out the
[sqlx-cli](https://github.com/transact-rs/sqlx/tree/main/sqlx-cli#create-and-run-migrations)
documentation for migrations database.
