use crate::state::Config;

/// The web application.
pub struct App {}

impl App {
    /// Create the web application.
    pub async fn bootstrap(_config: Config) -> Self {
        todo!()
    }

    /// Serve the application.
    pub async fn serve(self) {
        todo!()
    }
}
